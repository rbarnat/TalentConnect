<%= form_tag talent_appointment_charges_path(model.talent, model, price: model.talent.price) do %>
  <article>
    <% if flash[:error].present? %>
      <div id="error_explanation">
        <p><%= flash[:error] %></p>
      </div>
    <% end %>
    <label class="amount">
      <span>Total: <%= model.talent.price %> €</span>
    </label>
  </article>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"                 
                data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
                data-label="Payer"
                data-description="Payer"
                data-currency="EUR"
                data-amount=<%= model.talent.price*100 %>
                data-locale="fr"></script>
<% end %>